{
  "annotations": {"list": []},
  "editable": false,
  "panels": [
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {"unit": "reqps"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
      "id": 1,
      "targets": [
        {"expr": "sum(rate(portfolio_http_requests_total[1m]))", "legendFormat": "requests"}
      ],
      "title": "Request rate",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {"unit": "s"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
      "id": 2,
      "targets": [
        {
          "expr": "rate(portfolio_http_request_duration_seconds_sum[1m]) / rate(portfolio_http_request_duration_seconds_count[1m])",
          "legendFormat": "average"
        }
      ],
      "title": "Request duration",
      "type": "timeseries"
    }
  ],
  "refresh": "5s",
  "schemaVersion": 41,
  "tags": ["portfolio", "factory-demo"],
  "templating": {"list": []},
  "time": {"from": "now-15m", "to": "now"},
  "title": "Portfolio API",
  "uid": "portfolio-api",
  "version": 1
}
